<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>VideoRouter — Login</title>
  <link rel="stylesheet" href="/public/style.css">
  <script src="/public/eye.js"></script>
</head>
<body>
<main>
  <div class="card login-box">
    <h1>VideoRouter</h1>
    <% if (error) { %><p class="badge err"><%= error %></p><% } %>
    <form method="post" action="/login">
      <label>Email</label>
      <input type="email" name="email" required autofocus>
      <label>Password</label>
      <input type="password" name="password" required>
      <p><button class="primary" type="submit">Log in</button></p>
    </form>
    <% if (resetEnabled) { %><p class="muted"><a href="/reset">Forgot password?</a></p><% } %>
  </div>
</main>
</body>
</html>
